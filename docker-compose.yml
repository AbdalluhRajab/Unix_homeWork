services:
  web:
    build: .
    restart: always
    ports:
      - "8090:80"
    volumes:
      - .:/var/www/html
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sum_app
      MYSQL_USER: sumuser
      MYSQL_PASSWORD: 12345
    volumes:
      - ./db.sql:/docker-entrypoint-initdb.d/db.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "sumuser", "-p12345"]
      interval: 10s
      timeout: 5s
      retries: 10